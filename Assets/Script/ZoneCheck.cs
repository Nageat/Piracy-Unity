using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ZoneCheck : MonoBehaviour
{
    public bool IsAObjectOnIt;
    // Start is called before the first frame update
    void Start()
    {
        IsAObjectOnIt = false;
        //Debug.Log("Start");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
